export enum ESprintStatus {
  ACTIVE = 'ACTIVE',
  PENDING = 'PENDING',
  CLOSED = 'CLOSED',
}

export enum ESprintDuration {
  ONE_WEEK = '1 week',
  TWO_WEEKS = '2 weeks',
  THREE_WEEKS = '3 weeks',
  FOUR_WEEKS = '4 weeks',
  CUSTOM = 'custom',
}
