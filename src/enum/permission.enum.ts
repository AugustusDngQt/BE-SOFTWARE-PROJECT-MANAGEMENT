export enum EPermissionMethods {
  GET = 'Get',
  POST = 'Post',
  DELETE = 'Delete',
  PATCH = 'Patch',
}

export enum EPermissionModules {
  PROJECT = 'projects',
  SPRINT = 'sprints',
  ISSUE = 'issues',
  COMMENT = 'comments',
  MEMBER = 'members',
}
